import Layout from './components/layout/Layout'

function App() {
  return (
    <Layout>
      <section className="max-w-5xl mx-auto px-6 py-24">
        <h1 className="font-display text-4xl md:text-6xl font-semibold text-text-primary">
          Phase 1 — Foundation Ready
        </h1>
        <p className="mt-4 text-text-muted font-mono text-sm">
          Hero section arrives in Phase 2.
        </p>
      </section>
    </Layout>
  )
}

export default App
